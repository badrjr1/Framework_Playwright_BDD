services:
  jenkins:
    build: .
    container_name: jenkins-playwright-bdd
    ports:
      - "8080:8080"
      - "50000:50000"
    shm_size: "2gb"
    volumes:
      - jenkins_home:/var/jenkins_home

volumes:
  jenkins_home: